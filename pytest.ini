[pytest]
# Makes `src/` importable for `pytest` without requiring PYTHONPATH=src to be
# set manually first (that's still needed to run `python -m aico...` directly,
# see README Setup section).
pythonpath = src
testpaths = tests
